<!doctype html>
<html>
<head>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }
        body {
            text-align: center;
        }
        ul {
            width: 99vw;
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
        }
        li {
            list-style-type: none;
            border: 1px solid black;
            margin: 2px;
            display: flex;
            flex-direction: column;
        }
        h2 {
            font-size: 1em;
            border-bottom: 1px solid black;
        }
        img {
            max-width: 100%;
            max-height: 70%;
            margin: auto;
        }
        @media screen and (max-width: 300px) {
            li { flex: 0 0 90vw; }
        }
        @media screen and (min-width: 300px) and (max-width: 700px) {
            li { flex: 1 0 30vw; }
        }
        @media screen and (min-width: 700px) {
            li { flex: 1 0 200px; }
        }
    </style>
</head>
<body>
    <ul>
        <% trans.forEach(function ({ id, meanings }) { %>
            <li>
                <h2><%=meanings[0] %></h2>
                <img src="/img/<%=id %>.png" />
                <p>📝</p>
            </li>
        <% }); %>
    </ul>
</body>
</html>
